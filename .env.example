# 🚀 Claude Code Telemetry Configuration
# Copy this file to .env and add your Langfuse credentials

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# REQUIRED: Langfuse API Credentials
# Get from your local Langfuse instance at http://localhost:3000
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# OPTIONAL: Advanced Configuration
# Default values work for most users
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Langfuse endpoint (default: local instance)
LANGFUSE_HOST=http://localhost:3000

# Server port (default: 4318 - standard OTLP port)
# OTLP_RECEIVER_PORT=4318

# Server host (default: 127.0.0.1 - localhost only)
# OTLP_RECEIVER_HOST=127.0.0.1

# Session timeout in milliseconds (default: 3600000 = 1 hour)
# SESSION_TIMEOUT=3600000

# Maximum request size in bytes (default: 10485760 = 10MB)
# MAX_REQUEST_SIZE=10485760

# Optional API key for authentication (uncomment to enable)
# API_KEY=your-secret-api-key

# Logging level: debug, info, warn, error (default: info)
# LOG_LEVEL=info

# Environment: development, production (default: production)
# NODE_ENV=production

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# OPTIONAL: OTLP Export to OpenTelemetry Collector
# Forward telemetry to Jaeger, Prometheus, etc.
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Enable OTLP export (default: false)
# OTLP_EXPORT_ENABLED=true

# OpenTelemetry Collector endpoint
# OTLP_EXPORT_ENDPOINT=http://localhost:4317

# Override endpoints for specific signals (optional)
# OTLP_EXPORT_METRICS_ENDPOINT=http://localhost:4318/v1/metrics
# OTLP_EXPORT_LOGS_ENDPOINT=http://localhost:4318/v1/logs

# Request timeout in milliseconds (default: 5000)
# OTLP_EXPORT_TIMEOUT=5000

# Number of retries on failure (default: 3)
# OTLP_EXPORT_RETRIES=3

# Authentication headers (comma-separated key=value pairs)
# OTLP_EXPORT_HEADERS=Authorization=Bearer token,X-API-Key=secret

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# OPTIONAL: Custom Langfuse Trace Configuration
# Customize how traces appear in Langfuse
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Custom trace name
# LANGFUSE_TRACE_NAME=my-project

# Tags for filtering (semicolon, pipe, or comma separated)
# LANGFUSE_TRACE_TAGS=project-alpha;backend;refactoring

# Custom metadata as JSON
# LANGFUSE_TRACE_METADATA={"project":"alpha","sprint":12}

# Custom user ID
# LANGFUSE_USER_ID=developer@company.com

# Custom session ID
# LANGFUSE_SESSION_ID=sprint-12-auth-feature